# -*- mode: makefile -*-

# We need dummy variables so we don't fail if someone appends to them
# with +=
GPATH=
TARGET_DIR=
SETUP_FILES=
EXTRA_DIST=
DOC_STAMPS=
SCANOBJ_FILES=
REPORT_FILES=
CLEANFILES=
